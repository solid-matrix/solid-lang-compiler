namespace app;

func sum_to_n(n: i32): i32 {
    var total: i32 = 0;
    var i: i32 = 0;
    while i <= n {
        total += i;
        i += 1;
    }
    return total;
}

func main(): i32 {
    return sum_to_n(10);
}
