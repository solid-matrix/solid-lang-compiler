const MAX_FRAME_COUNT = 32;

const MAX_BUFFER_SIZE: usize = 4096;

static FrameCount = 0;

static BufferCount: u32 = 0;

struct Size{
    width: u32,
    height: u32,
}


func main():i32{
    const MAX_SIZE: Size =  Size{width = 800, height = 600};

    const MAX_SIZE_SIDEBAR = Size{width = 400, height = 300};

    static size_count = 0;

    static size_siderbar_count: u32 = 1;

    var a = 0;

    var b: i32 = 0;

    var c: Size = Size{width = 1, height = 2};

}
